{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ -f website/package.json ]; then npm --prefix website run build && rm -rf build && cp -R website/build build; else npm run build; fi",
  "outputDirectory": "build",
  "installCommand": "npm ci"
}
