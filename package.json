{
  "name": "openset",
  "private": true,
  "description": "An open, sport-agnostic standard for representing structured training workouts as machine-readable data",
  "workspaces": [
    "packages/*",
    "website"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
